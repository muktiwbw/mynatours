header.header
  nav.nav.nav--tours
    a.nav__el(href='/') Natours
    form.nav__search
      button.nav__search-btn
        svg: use(xlink:href='/img/icons.svg#icon-search')
      input.nav__search-input(type='text', placeholder='Search tour')
  .header__logo
    img(src="/img/logo-white.png", alt="Natours logo")
  nav.nav.nav--user
    // Logged in
    //- a.nav__el(href="#") My bookings
    //- a.nav__el(href="#")
    //-   img.nav__user-img(src="/img/users/default.jpg", alt="User photo")
    //-   span Mukti
    
    // Guest
    button.nav__el Login
    button.nav__el.nav__el--cta Register